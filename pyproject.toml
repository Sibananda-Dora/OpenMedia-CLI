[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "openmedia"
version = "1.1.0"
dependencies = [
    "requests",
    "rich",
    "questionary",
    "pydantic",
    "langgraph",
]

[project.scripts]
openmedia = "openmedia.cli_frontend:main"
